#include "main.h"
/**
 * _isupper - letters
 * @c: char checking upper and lower case letters
 *
 * Return: 0 or 1
 */
int _isupper(int c)
{
        	if (c >= 'A' && c <= 'Z')
                    	return (1);
        	else
        	return (0);
}

